export type AccountCartDialogProps = {
  open: boolean;
  setOpen: (value: boolean) => void;
  data: any;
};

export type AccountDialogProps = {
  open: boolean;
  setOpen: (value: boolean) => void;
};